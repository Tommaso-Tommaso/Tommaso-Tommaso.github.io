// Companion runtime file for local preview. This mirrors the minimal published main.js.
(function(){
  // If you have typescript for site behavior, compile it to scripts/main.js for production.
})();
